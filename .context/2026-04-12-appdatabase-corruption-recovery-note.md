# AppDatabase Corruption Recovery Note

- Add a user-facing recovery button or safe startup recovery path for `AppDatabase` failures like `SQLite error 26: file is not a database` so corrupted local DB files can be deleted and recreated intentionally.
