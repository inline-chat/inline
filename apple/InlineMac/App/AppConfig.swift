// Use AppConfig for internal flags, app config, and simple code-only switches
// that should not be persisted as user settings.
struct AppConfig {
  static let macMessageAvatarOverlayEnabled = true
}
