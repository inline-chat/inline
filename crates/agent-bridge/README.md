# Inline Agent Bridge

Provider-neutral contracts, state machines, presentation policy, and durable
state for Inline's local coding-agent bridge.

Provider wire protocols live in separate driver crates. CLI setup and service
installation remain in the `inline` binary.
