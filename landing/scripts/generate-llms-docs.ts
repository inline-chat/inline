// Public Markdown and llms.txt are served dynamically from the authored docs catalog.
// Keep this compatibility command as a validation alias for older local workflows.
await import("./check-docs")

export {}
