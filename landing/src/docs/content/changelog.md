# What's New

Release notes for Inline app updates.

We keep older entries available so users can find exact build numbers, previous macOS downloads, and the context for each release.

## Release Notes Coming Soon

The first public changelog entry is being prepared.

Future entries will include:

- A short summary for TestFlight and the Inline changelog thread.
- Exact iOS TestFlight build numbers.
- Exact macOS build numbers and DMG links when available.
- Images or video links for visual changes when useful.

For current install links, see [Downloads](/docs/downloads).
